import React from 'react'
import { DefaultTemplate } from 'components/templates'
import { TaskList } from 'modules'

const Home = () => (
  <DefaultTemplate>
    <TaskList />
  </DefaultTemplate>
)

export default Home
